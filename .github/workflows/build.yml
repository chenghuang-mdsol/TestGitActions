name: CI

on: 
  push:
    branches:
      - 'docker'

jobs:
 R_latest:
   name: Test on latest
   runs-on: windows-latest
   #container: 
   #   image: cr.microsoft.com/windows/servercore:ltsc2019
   steps:
    - name: login docker
      run: docker login mdsol-rave-containers.jfrog.io -u rave-ci -p 'AKCp5dK4qUwF1k5pfoPFUb1fEv7JQLqFK86JqBQciBRB8MtpMHp4fv8KbYHoLbAQ72Q3NpsnY'
      shell: powershell
    - name: pull docker
      run: docker run -d mdsol-rave-containers.jfrog.io/rave-containers/ci/buildtools2019/latest powershell;
      shell: powershell
   
    
